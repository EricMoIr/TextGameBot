const config = {
    "prefix": process.env.BOT_PREFIX,
    "token": process.env.BOT_TOKEN,
    "connectionString": "mongodb://localhost/test",//process.env.CONNECTION_STRING,
}

module.exports = config;